[Unit]
Descript=Consul Daemon
After=syslog.target

[Service]
PIDFile=/var/run/consul.pid
ExecStart=/usr/bin/consul agent -config-dir /etc/consul.d/<% if @server -%>server<% else -%>client<% end -%>

[Install]
WantedBy=multi-user.target
